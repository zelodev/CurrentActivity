name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      APP_FOLDER: app
    steps:
    - uses: actions/checkout@v3
    - name: Build and Release APK
      uses: r0user/release-apk@V1.0
            
